# Dad Cam Pipeline - Requirements
# ================================

# Core dependencies
numpy>=1.20.0
scipy>=1.7.0

# TransNet V2 shot detection (optional but recommended)
tensorflow>=2.6.0
Pillow>=8.0

# Optional: Install transnetv2 package for better shot detection
# pip install git+https://github.com/soCzech/TransNetV2.git

# Note: The following system tools must be installed:
# - ffmpeg (with libx265, vidstab support)
# - exiftool
#
# On macOS:
#   brew install ffmpeg exiftool
#
# On Ubuntu/Debian:
#   apt install ffmpeg libimage-exiftool-perl
